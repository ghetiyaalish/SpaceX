<odoo>
    <!-- existing code -->
    <record id="module_sa_crm_min_dashboard"  model="ir.module.category">
        <field name="name">CRM Mini Dashboard</field>
        <field name="description">CRM Mini Dashboard</field>
    </record>

    <record id="group_sa_crm_min_dashboard" model="res.groups">
        <field name="name">View CRM Dashboard</field>
        <field name="category_id" ref="module_sa_crm_min_dashboard"/>
    </record>

    <!-- <record id="account_move_report_rule" model="ir.rule">
        <field name="name">Invoice Report Access</field>
        <field name="model_id" ref="model_account_move"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record> -->

    <!-- Additional groups for the specific permissions you mentioned -->
   <!-- <record id="account_move_invoice_rule" model="ir.rule">
    <field name="name">Invoice Creation from CRM</field>
    <field name="model_id" ref="account.model_account_move"/>
    <field name="domain_force">[]</field>
    <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
    </record>

    <record id="group_crm_team_view" model="res.groups">
        <field name="name">View Sales Teams</field>
        <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        <field name="category_id" ref="module_sa_crm_min_dashboard"/>
    </record> -->

</odoo>
